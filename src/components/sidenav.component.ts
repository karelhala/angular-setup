import { Component } from '@angular/core';

@Component({
  selector: 'side-nav',
  template: require('./sidenav.component.html'),
  styles: [require('./sidenav.component.scss').toString()]
})
export class SideNav {
}
